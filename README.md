# Slope Motion System

A classroom tool for real‑time motion graphing and a metronome.

## Setup
1. Clone the repo
2. Run `npm install`
3. Start the server with `npm start`
4. Open `http://localhost:3000`

## Structure
- `server/` – Express + Socket.IO backend
- `public/` – Frontend (teacher, student, metronome)